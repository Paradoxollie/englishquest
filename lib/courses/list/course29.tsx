import { LessonContent, LessonSection, PlaceholderContent } from "@/lib/courses/types";
import { Quiz } from "@/components/courses/quiz";
import { AlertIcon, InfoIcon, ArrowRightIcon } from "@/components/ui/icons";
import { ReactNode } from "react";

export const course29: LessonContent = { courseNumber: 29, title: "Probabilité (May/Might)", objective: "Il est possible que...", sections: [{ title: "En construction", content: <PlaceholderContent /> }] };