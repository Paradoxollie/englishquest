import { LessonContent, LessonSection, PlaceholderContent } from "@/lib/courses/types";
import { Quiz } from "@/components/courses/quiz";
import { AlertIcon, InfoIcon, ArrowRightIcon } from "@/components/ui/icons";
import { ReactNode } from "react";

export const course31: LessonContent = { courseNumber: 31, title: "First Conditional", objective: "If + Present + Will.", sections: [{ title: "En construction", content: <PlaceholderContent /> }] };